language: php
addons:
  apt:
    packages:
      - locales
      - language-pack-id
php:
  - 5.6
  - 7.0
before_script:
  - "locale -a | grep id"
  - "composer install"
script:
  - "vendor/bin/phpunit"